{
  "framework": null,
  "installCommand": "corepack enable && corepack prepare pnpm@10.33.2 --activate && cd mobile && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.33.2 --activate && cd mobile && pnpm run build:web",
  "outputDirectory": "mobile/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
