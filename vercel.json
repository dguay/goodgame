{
  "framework": null,
  "installCommand": "cd mobile && pnpm install --frozen-lockfile",
  "buildCommand": "cd mobile && pnpm run build:web",
  "outputDirectory": "mobile/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
